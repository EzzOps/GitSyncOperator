---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: configmapsyncs.sync.github.com
spec:
  group: sync.github.com
  names:
    kind: ConfigMapSync
    listKind: ConfigMapSyncList
    plural: configmapsyncs
    singular: configmapsync
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.configMapName
      name: ConfigMap
      type: string
    - jsonPath: .spec.gitURL
      name: Repo
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConfigMapSync is the Schema for the configmapsyncs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConfigMapSyncSpec defines the desired state of ConfigMapSync
            properties:
              configMapName:
                description: ConfigMapName is the name of the ConfigMap to sync.
                minLength: 1
                type: string
              configMapNamespace:
                description: |-
                  ConfigMapNamespace is the namespace of the ConfigMap to sync.
                  If empty, the ConfigMap is assumed to be in the same namespace as the ConfigMapSync.
                type: string
              gitBranch:
                description: GitBranch is the git branch to sync to.
                type: string
              gitSecretName:
                description: GitSecretName is the name of a Secret containing git
                  credentials.
                type: string
              gitSecretPassword:
                description: GitSecretPassword is the key in the Secret that contains
                  the password.
                type: string
              gitSecretUsername:
                description: GitSecretUsername is the key in the Secret that contains
                  the username.
                type: string
              gitURL:
                description: GitURL is the HTTPS repository URL to sync to.
                minLength: 1
                type: string
              repoPath:
                description: RepoPath is an optional path inside the repo to root
                  the target path.
                type: string
              targetPath:
                description: |-
                  TargetPath is the path inside the repository file tree where the ConfigMap YAML will be written.
                  If omitted, a default path under configmaps/ is used.
                type: string
            required:
            - configMapName
            - gitURL
            type: object
          status:
            description: ConfigMapSyncStatus defines the observed state of ConfigMapSync
            properties:
              conditions:
                description: Conditions contains status conditions for this ConfigMapSync.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastCommit:
                description: LastCommit is the last pushed git commit hash.
                type: string
              lastHash:
                description: LastHash is the last synced content hash of the referenced
                  ConfigMap.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
